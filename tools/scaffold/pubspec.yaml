name: scaffold
description: Create a new project from the Pattern H skeleton
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.5.0
  path: ^1.9.0
  yaml: ^3.1.0

dev_dependencies:
  test: ^1.25.0
